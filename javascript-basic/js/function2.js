//与えられた引数numを2倍にし、その値を戻り値として返す関数を定義
const double = (num) => {
	return num * 2;
}
//関数の戻り値を出力する
console.log(double(30));

//定数を宣言する
const userName = '侍太郎'3
//定数の値を出力する
console.log(userName);

//関数の中で定数を使う
const useVariable = () =>{
	console.log(userName);
}
//関数を呼び出す
useVariable();